# Frontend environment variables
# Copy this file to .env.local or set the values in your deployment provider.

# The backend API origin used by the Vite app.
# Use a full absolute URL in production, for example:
# VITE_API_BASE_URL=https://api.your-domain.com/api
VITE_API_BASE_URL=/api