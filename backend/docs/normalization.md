# Normalization Engine

Overview of the normalization engine and available developer utilities.

Toggle heavy model usage in quick CI or development runs:

```
NORMALIZATION_DEBUG_MODE=true
```

CLI helper:

```
python backend/scripts/normalize_sample.py Python JS "Machine Learning"
```
